/* Beau's Game Inventory navigation — stable direct-link build v5.1.0 */
(function(){
'use strict';
const items=[['Dashboard','dashboard'],['Inventory','inventory'],['Scanner','scanner'],['Pricing','pricing'],['Library','library'],['Settings','settings']];
const svg={dashboard:'📊',inventory:'📦',scanner:'📷',pricing:'💰',library:'📚',settings:'⚙️'};
const pages={};
function getContainer(){return document.querySelector('.container')}
function pageForPanel(panel){const t=(panel.querySelector('h2')?.textContent||'').trim().toLowerCase();if(t.includes('quick sale')||t.includes('sales history')||t.includes('record sale')||t==='sales'||t.includes('sell inventory')){panel.remove();return null}if(t.includes('barcode scanner')||t.includes('scanner'))return 'scanner';if(t.includes('price comparison')||t.includes('pricing')||t.includes('market price'))return 'pricing';if(t.includes('library')||t.includes('scanned games'))return 'library';if(t.includes('backup')||t.includes('settings')||t.includes('appearance')||t.includes('updates'))return 'settings';if(t.includes('add / edit item')||t==='📦 inventory'||t.includes('inventory'))return 'inventory';return null}
function buildPages(){const c=getContainer();if(!c)return;if(c.querySelector(':scope > .app-page')){cachePages();return}const buckets={dashboard:[],inventory:[],scanner:[],pricing:[],library:[],settings:[]};const d=c.querySelector(':scope > .dashboard');if(d)buckets.dashboard.push(d);[...c.querySelectorAll(':scope > .panel')].forEach(p=>{const k=pageForPanel(p);if(k)buckets[k].push(p)});Object.entries(buckets).forEach(([k,els])=>{const p=document.createElement('main');p.className='app-page';p.id='page-'+k;els.forEach(e=>p.appendChild(e));c.appendChild(p)});cachePages()}
function cachePages(){items.forEach(([,k])=>pages[k]=document.getElementById('page-'+k))}
function activate(k){if(!pages[k])k='dashboard';items.forEach(([,name])=>{const p=pages[name];if(p)p.classList.toggle('active',name===k)});document.querySelectorAll('.mobile-nav button').forEach(b=>b.classList.toggle('active',b.dataset.screen===k));history.replaceState(null,'','#'+k)}
function mount(){buildPages();document.querySelectorAll('.mobile-nav,.app-shell-nav,.nav').forEach(n=>n.remove());const n=document.createElement('nav');n.className='mobile-nav';n.innerHTML=items.map(([label,k])=>`<button type="button" data-screen="${k}" aria-label="${label}"><span class="icon" style="font-size:21px;line-height:22px">${svg[k]}</span><span class="label">${label}</span></button>`).join('');document.body.appendChild(n);n.querySelectorAll('button').forEach(b=>b.addEventListener('click',()=>{const k=b.dataset.screen;if(k==='scanner'){window.location.assign('./scanner-v3.html?v=3.2.0');return}if(k==='library'){window.location.assign('./library-v3.html?v=5.1.0');return}activate(k)}));window.BeauNavigation={activate,current:null};const hash=(location.hash||'').replace('#','').split('?')[0];activate(items.some(x=>x[1]===hash)?hash:'dashboard')}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount();
})();
