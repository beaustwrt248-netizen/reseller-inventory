# Reseller Hub v6.1 Restore Point

Protected baseline commit: `273bf0c9e9b02dfb337e3cd7ac2e682021d6d88a`

This branch preserves the v6.1 application state before further redesign work.

The backup covers repository layouts, scripts, theme files, navigation, scanner, pricing engine, settings, service-worker files and configuration at the restore point.

Browser-local inventory/library data is not stored in GitHub. Use the app's export function to back up `resellerInventory` and `beauGameLibrary` separately.
